import { getAllPost, getPost, getImage, getDocument, likeOrDislikeNews, loadingPost, errorPost, deleteComment, categoryPost, commentPost } from '../types/postType';
import { apiUrl } from '../../App';